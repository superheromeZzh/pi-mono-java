pi:
  assistant:
    task:
      base-dir: ./data/tasks

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pi_assistant?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true

jobrunr:
  background-job-server:
    enabled: true
  dashboard:
    enabled: false
