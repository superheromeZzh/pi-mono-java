# Rsync exclude patterns for sync-mate-campusclaw.sh.
# Paths are interpreted relative to the rsync source root (build/mate-campusclaw/src/.../).
# Lines starting with '#' are comments; blank lines ignored.
#
# Files listed here exist only on the mate-campusclaw side (added directly,
# not derived from modules/*). The sync script must NOT overwrite or delete them.
# When you intentionally add a new file under mate-campusclaw/ that has no
# counterpart in modules/*, add its path here so future syncs preserve it.

# Java code only on mate side
/com/huawei/hicampus/mate/matecampusclaw/assistant/config/
/com/huawei/hicampus/mate/matecampusclaw/codingagent/channel/
