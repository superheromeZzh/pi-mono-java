# examples（静态样例）

本目录仅保留**文档用**样例，不存放业务 Excel。

| 文件 | 用途 |
|------|------|
| `compile_report_fault_rules_sheet2.json` | `compile_report.json` 失败形态示例 |

用户的 `故障规则.xlsx` 放在 **workspace**（如 OpenClaw），改表另存为 `故障规则_patched.xlsx`，勿提交进 skill。

完整 skill 备份（重构前）：`../excel-antlr-to-rules-json.backup-20260519/`
