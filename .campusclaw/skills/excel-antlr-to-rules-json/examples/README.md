# examples（静态样例）

本目录仅保留**文档用**虚构样例，不存放业务 Excel。

**注意**：样例中的设备、点位、规则 ID 均为**冷冻站教学虚构**，与真实 `故障规则.xlsx` 无关，避免模型照抄示例改表。

| 文件 | 用途 |
|------|------|
| `compile_report_example_sheet2.json` | `compile_report.json` 失败形态示例（虚构 `lab_equipment_rules.xlsx`） |

用户的 `故障规则.xlsx` 放在 **workspace**（如 OpenClaw），改表另存为 `故障规则_patched.xlsx`，勿提交进 skill。

完整 skill 备份（重构前）：`~/.openclaw/backups/excel-antlr-to-rules-json.backup-20260519/`
