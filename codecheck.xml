<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- 让 @SuppressWarnings("checkstyle:xxx") 注解能抑制对应规则的违规。
         需要 TreeWalker 内的 SuppressWarningsHolder 配合。 -->
    <module name="SuppressWarningsFilter"/>

    <!-- FileHeaderComment: every .java file must start with the Huawei copyright header.
         Year range \d{4}-\d{4} accepts e.g. 2026-2026 or 2024-2026. -->
    <module name="RegexpHeader">
        <property name="header"
                  value="^/\*$\n^ \* Copyright \(c\) Huawei Technologies Co\., Ltd\. \d{4}-\d{4}\. All rights reserved\.$\n^ \*/$"/>
        <property name="fileExtensions" value="java"/>
    </module>

    <!-- comment_blank_line_before: 行注释 // 紧跟在以 `;` 或 `}` 结尾的代码行下方时，
         中间必须空一行——把「段落标题式注释」与上一段代码视觉分开。两种触发形态：
         (1) 上一行以 `;`/`}` 结尾，无 trailing 注释；
         (2) 上一行以 `;`/`}` 结尾后跟 `// trailing...`，下一行是另一段 `//`——上一行
             的 trailing 注释属于上一行那条语句，与下一段段落注释是两件事，仍需空行分开。
         匹配示例（BAD）：
             int x = 1;
             // next section
         以及（BAD）：
             foo(); // does the thing
             // next paragraph describing the next block
         应当写为（GOOD）：
             int x = 1;

             // next section
         以及（GOOD）：
             foo(); // does the thing

             // next paragraph describing the next block
         排除：
         - `///` 三斜线（DocC-style）；
         - 上一行以 `{`、`(`、`,` 等结尾——块内开场或参数延续不属于「新段落」。
         由 RegexpMultiline 在 Checker 级跨行扫描。 -->
    <module name="RegexpMultiline">
        <property name="id" value="comment_blank_line_before"/>
        <property name="format" value="[;}](?:[ \t]*//[^\r\n]*)?[ \t]*\r?\n[ \t]*//[^/\r\n]"/>
        <property name="fileExtensions" value="java"/>
        <property name="message" value="独占一行的 // 注释紧跟在 ; 或 } 之后时（即便上一行带 trailing //），请在注释行上方空一行。"/>
    </module>

    <!-- javadoc_blank_line_before: Javadoc 块 /** 紧跟在以 `;` 或 `}` 结尾的代码行下方时，
         中间必须空一行——和 comment_blank_line_before 同源同理，把成员声明与紧贴它上方的
         上一个成员视觉分开。最常见的反例是「字段 + Javadoc + 字段 + Javadoc」连排时漏写空行，
         读者一眼看不出 Javadoc 是「上个字段的尾巴」还是「下个字段的标题」。
         匹配示例（BAD）：
             private final ObjectMapper mapper;
             /**
              * Doc for next field.
              */
             private final ObjectWriter writer;
         应当写为（GOOD）：
             private final ObjectMapper mapper;

             /**
              * Doc for next field.
              */
             private final ObjectWriter writer;
         由 RegexpMultiline 跨行扫描；class/method/field 的首个 Javadoc 紧贴 `{` 不会命中
         （`{` 不在前置字符集合里）。 -->
    <module name="RegexpMultiline">
        <property name="id" value="javadoc_blank_line_before"/>
        <property name="format" value="[;}][ \t]*\r?\n[ \t]*/\*\*"/>
        <property name="fileExtensions" value="java"/>
        <property name="message" value="Javadoc /** 紧跟在 ; 或 } 之后时，请在 Javadoc 上方空一行——视觉分离上一个成员与下一段文档。"/>
    </module>

    <!-- no_back_to_back_javadoc: 两段 Javadoc `*/` 后紧跟 `/**` 视为非法。
         成因通常是：方法被移动 / 重命名后旧 Javadoc 滞留，或者重复生成；
         此时只有第二段会绑定到下一个声明，第一段悬空（Javadoc 工具忽略，但读者会困惑）。
         发现违规时应当：保留更准确的一段 / 把 Javadoc 移到目标声明正上方。 -->
    <module name="RegexpMultiline">
        <property name="id" value="no_back_to_back_javadoc"/>
        <property name="format" value="\*/[ \t]*\r?\n[ \t]*/\*\*"/>
        <property name="fileExtensions" value="java"/>
        <property name="message" value="检测到相邻的两段 Javadoc 块——上一段已悬空。请删除冗余的一段，或把 Javadoc 移到目标声明正上方。"/>
    </module>

    <!-- no_silent_catch: [[no_empty_catch_block]] 的加强版——把「只有注释、没有任何实际处理
         语句」的 catch 体也拒。理由：留一行 "// best-effort" 而不打日志只是 signed silence——
         对运行时可观测性是零，故障溯源仍然得靠猜，达不到 no_empty_catch_block 想堵的根本目的。
         合规写法：
         1. `log.debug/warn/error("contextual message", e)` 走 SLF4J；
         2. `Thread.currentThread().interrupt()` 恢复中断位（吞 InterruptedException 必备）；
         3. `throw new ...Exception(e)` 包装重抛；
         4. 状态修复（`flag = true`、`map.put(...)`、`return defaultValue` 等）。
         检测：RegexpMultiline 跨行匹配 `catch (...) { 仅空白和注释 }`——具体地，
         body 内只允许：空白 (`\s`)、行注释 (`//...`)、块注释 (`/* ... */`)。一旦出现任何
         其他 token（标识符、字面量、关键字），就视为有实际处理，规则放行。 -->
    <module name="RegexpMultiline">
        <property name="id" value="no_silent_catch"/>
        <property name="format" value="catch\s*\([^)]*\)\s*\{(?:\s|//[^\r\n]*|/\*[\s\S]*?\*/)*\}"/>
        <property name="fileExtensions" value="java"/>
        <property name="message" value="catch 体不能只有注释——请走 SLF4J 打日志（log.debug/warn/error(&quot;...&quot;, e)）、Thread.currentThread().interrupt() 恢复中断位、或重抛包装异常。"/>
    </module>

    <!-- Locale: 禁止 String.format 直接以字符串字面量为第一参数（即未指定 Locale）。
         允许 String.format(Locale.X, "...") 或 String.format(localeVar, "...")。
         用 RegexpMultiline 在 Checker 级跨行扫描，因此 palantir 把 `String.format(` 与格式
         串折成两行（常见格式）也会命中——这是相对 RegexpSinglelineJava 的关键差别。
         代价：无 ignoreComments，Javadoc 里举反例需要写成 `String . format(...)` 错位字符
         或加局部豁免。截至 2026-05-19 全仓零此类示例，落规则前已盘点过。 -->
    <module name="RegexpMultiline">
        <property name="id" value="LocaleStringFormat"/>
        <property name="format" value="\bString\.format\s*\(\s*&quot;"/>
        <property name="fileExtensions" value="java"/>
        <property name="message" value="String.format 必须显式以 Locale.ROOT 或 Locale.ENGLISH 作为第一参数。"/>
    </module>

    <module name="TreeWalker">
        <module name="SuppressWarningsHolder"/>

        <module name="NeedBraces">
            <property name="tokens" value="LITERAL_IF, LITERAL_ELSE, LITERAL_FOR, LITERAL_WHILE, LITERAL_DO"/>
        </module>

        <!-- BracesPosition: 左大括号放行尾，右大括号另起一行（K&R 衔接除外）。 -->
        <module name="LeftCurly">
            <property name="option" value="eol"/>
        </module>
        <!-- 多块语句的中间收尾：} 与 else/catch/finally/while-of-do 同行。 -->
        <module name="RightCurly">
            <property name="id" value="RightCurlySame"/>
            <property name="option" value="same"/>
            <property name="tokens" value="LITERAL_TRY, LITERAL_CATCH, LITERAL_FINALLY, LITERAL_IF, LITERAL_ELSE, LITERAL_DO"/>
        </module>
        <!-- 定义体与其他独立块的收尾：} 单独一行（允许单行块如空构造器 `Foo() {}`）。 -->
        <module name="RightCurly">
            <property name="id" value="RightCurlyAlone"/>
            <property name="option" value="alone_or_singleline"/>
            <property name="tokens" value="METHOD_DEF, CTOR_DEF, CLASS_DEF, INTERFACE_DEF, ENUM_DEF, RECORD_DEF, ANNOTATION_DEF, STATIC_INIT, INSTANCE_INIT, COMPACT_CTOR_DEF, LITERAL_FOR, LITERAL_WHILE, LITERAL_SWITCH"/>
        </module>

        <!-- Locale: 禁止无参数的 toLowerCase/toUpperCase，避免 Turkish 等 locale 引发的诡异 bug
             (例如 "I".toLowerCase() 在土耳其 locale 下变 "ı" 而非 "i")。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="LocaleCaseConversion"/>
            <property name="format" value="\.(toLowerCase|toUpperCase)\s*\(\s*\)"/>
            <property name="message" value="字符串大小写转换必须显式传入 Locale.ROOT 或 Locale.ENGLISH。"/>
            <property name="ignoreComments" value="true"/>
        </module>
        <!-- explicit_charset_*: 字符 ↔ 字节互转 API 必须显式传 Charset（统一 StandardCharsets.UTF_8）。
             覆盖最常见的「单凭语法即可判定无歧义错误」的几个调用点；语义模糊的（如
             new String(byte[]) vs new String(char[]) 类型相关、Files.readString 默认即 UTF-8）
             保留为 CLAUDE.md 中的软约束，靠 review 把关。
             检测思路：行内出现违规调用，且整行没有任何 Charset/UTF_x/ASCII/ISO_8859 字样
             即报错——`Charset.forName(...)` 或 `, StandardCharsets.UTF_8)` 都能算作豁免。
             副作用：调用被换行折断时（new InputStreamReader( 和参数分两行）可能误报，
             修复方式是把调用合并为单行（与 palantir 默认行为一致）。-->
        <module name="RegexpSinglelineJava">
            <property name="id" value="explicit_charset_get_bytes"/>
            <property name="format" value="\.getBytes\(\s*\)"/>
            <property name="message" value="字符串转字节数组必须显式传 Charset：用 .getBytes(StandardCharsets.UTF_8) 替代无参 .getBytes()。"/>
            <property name="ignoreComments" value="true"/>
        </module>
        <module name="RegexpSinglelineJava">
            <property name="id" value="explicit_charset_input_stream_reader"/>
            <property name="format" value="^(?=(?:(?!StandardCharsets|Charset|UTF_8|UTF_16|US_ASCII|ISO_8859).)*$).*\bnew\s+InputStreamReader\s*\("/>
            <property name="message" value="new InputStreamReader(...) 必须显式传 Charset（StandardCharsets.UTF_8）。"/>
            <property name="ignoreComments" value="true"/>
        </module>
        <module name="RegexpSinglelineJava">
            <property name="id" value="explicit_charset_output_stream_writer"/>
            <property name="format" value="^(?=(?:(?!StandardCharsets|Charset|UTF_8|UTF_16|US_ASCII|ISO_8859).)*$).*\bnew\s+OutputStreamWriter\s*\("/>
            <property name="message" value="new OutputStreamWriter(...) 必须显式传 Charset（StandardCharsets.UTF_8）。"/>
            <property name="ignoreComments" value="true"/>
        </module>
        <module name="RegexpSinglelineJava">
            <property name="id" value="explicit_charset_file_reader"/>
            <property name="format" value="^(?=(?:(?!StandardCharsets|Charset|UTF_8|UTF_16|US_ASCII|ISO_8859).)*$).*\bnew\s+FileReader\s*\("/>
            <property name="message" value="new FileReader(...) 必须显式传 Charset（StandardCharsets.UTF_8）。"/>
            <property name="ignoreComments" value="true"/>
        </module>
        <module name="RegexpSinglelineJava">
            <property name="id" value="explicit_charset_file_writer"/>
            <property name="format" value="^(?=(?:(?!StandardCharsets|Charset|UTF_8|UTF_16|US_ASCII|ISO_8859).)*$).*\bnew\s+FileWriter\s*\("/>
            <property name="message" value="new FileWriter(...) 必须显式传 Charset（StandardCharsets.UTF_8）。"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- no_system_out_err: 禁止把 System.out / System.err 当日志使用。
             所有「记录日志」诉求一律走 SLF4J（private static final Logger log = LoggerFactory.getLogger(...)）。
             少数 stdout/stderr 本就是接口的场景——Picocli CLI 用户输出、Print/Rpc 模式的协议输出、
             TUI 终端渲染、Server 启动 banner——必须在类声明上加
             @SuppressWarnings("checkstyle:no_system_out_err") 显式声明意图，让 reviewer
             一眼看出「这是协议/UI 输出，不是日志」。
             检测：行内出现 System.(out|err).(print|println|printf|format|append|write) 即报错；
             ignoreComments=true 让 javadoc / 注释里提及 System.out.println(...) 不被误判。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="no_system_out_err"/>
            <property name="format" value="\bSystem\.(out|err)\.(print|println|printf|format|append|write)\b"/>
            <property name="message" value="禁止用 System.out/System.err 打印日志：请改用 SLF4J（private static final Logger log = LoggerFactory.getLogger(...)）。如确属 CLI/协议/UI 输出，请在类上加 @SuppressWarnings(&quot;checkstyle:no_system_out_err&quot;)。"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- no_print_stack_trace: 禁止 Throwable.printStackTrace()，必须走 log.error("...", e)，
             让异常进入 SLF4J / logback / log4j 配置链——能被 appender 收集、能被测试静默、
             带上下文（logger 名、线程、级别、时间戳）。
             当前仓内存量已为 0，规则上锁防止回潮。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="no_print_stack_trace"/>
            <property name="format" value="\.printStackTrace\s*\("/>
            <property name="message" value="禁止 Throwable.printStackTrace()：请改用 log.error(&quot;...&quot;, e) 让异常进入日志框架。"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- no_chinese_in_log: 日志消息模板字面量禁含 CJK 字符或中日韩全角标点。
             CampusClaw 不只服务中文区客户——日志消费方包括海外 SRE、ELK/Splunk 全文检索、
             grep/awk 流水线、CI 截图、客户附带的 support bundle。一旦消息含中文：
             海外协作者读不懂、charset 误解码即不可恢复乱码、grep / logstash mutate 难以匹配、
             终端字体缺失或 BiDi 渲染下错位、合规归档非英文不便审计。
             检测：行内出现 (log|logger|LOGGER|LOG).(trace|debug|info|warn|error)( 且首个 String
             字面量内含 Unicode 4E00–9FFF（CJK 统一表意） / 3000–303F（CJK 符号标点） /
             FF00–FFEF（半角与全角形式）任意字符即报错。变量值通过 {} 占位符传入不受限——
             规则只管模板字面量。ignoreComments=true 让 javadoc 举反例不被误判。
             当前仓内存量已为 0，规则上锁防止回潮。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="no_chinese_in_log"/>
            <property name="format" value="\b(log|logger|LOGGER|LOG)\.(trace|debug|info|warn|error)\s*\(\s*&quot;[^&quot;]*[一-鿿　-〿＀-￯]"/>
            <property name="message" value="日志消息模板禁含中文字符 / 全角标点：请改写为英文（变量值通过 {} 占位符传入不受限）。"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- comment_marker_space: 行首注释符 // 与 /* 之后必须紧跟空白（空格/制表符/换行）
             或者重复同样的符号（`///` 三斜线、`/**` Javadoc 起始）。
             正例：`// foo`、`/** ... */`、`/* foo */`、`//` 空注释、`///`、`/**`。
             反例：`//foo`、`/*foo*/`。
             理由：注释符紧贴注释内容（`//foo`）极大降低可读性，IDE 折叠/语法着色
             也会失效。空格之后再写文字是社区惯例（Google / Sun / Oracle 风格指南均要求）。
             检测范围：仅匹配「行首空白前缀 + 注释符」的情况——避免字符串字面量中
             `://`、`-//Apple//DTD ...`、shell 脚本里的 `s/foo//` 等被误判。
             行尾追加的 trailing 注释（如 `int x = 1; //foo`）不在此规则覆盖范围内，
             以代码评审把关。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="comment_marker_space"/>
            <property name="format" value="^[ \t]*(?://[^/ \t]|/\*[^* \t])"/>
            <property name="message" value="注释符与注释内容之间必须有空格：// 后留空格再写内容（/* 同理）。"/>
            <property name="ignoreComments" value="false"/>
        </module>

        <!-- non_constant_field_camel_case: 非常量字段必须 lowerCamelCase。
             覆盖两类字段：
             - 实例字段（MemberName）：`private String userName`、`int retryCount` 等
             - 静态非 final 字段（StaticVariableName）：`private static Logger log` 等
             不覆盖 `static final` 常量（ConstantName 的领域，通常 UPPER_SNAKE_CASE）。
             正则 `^[a-z][a-zA-Z0-9]*$`：必须小写字母开头，纯字母数字，禁止下划线/美元号。
             ❌ 反例：`user_name`、`UserName`、`_count`、`m_value`。
             ✅ 正例：`userName`、`retryCount`、`isActive`、`log`。 -->
        <module name="MemberName">
            <property name="id" value="non_constant_field_camel_case_instance"/>
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="StaticVariableName">
            <property name="id" value="non_constant_field_camel_case_static"/>
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <!-- 局部变量也按 lowerCamelCase（含 final 局部变量）；for 循环单字符变量豁免。 -->
        <module name="LocalVariableName">
            <property name="id" value="non_constant_field_camel_case_local"/>
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
            <property name="allowOneCharVarInForLoop" value="true"/>
        </module>
        <module name="LocalFinalVariableName">
            <property name="id" value="non_constant_field_camel_case_local_final"/>
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>

        <!-- no_fake_assertion_*: 测试代码必须包含**真实**的断言检查，禁止「虚假断言」。
             虚假断言指语义上恒成立、无法验证被测代码行为的断言——它在 CI 上永远绿，
             看似有覆盖率实则什么都没测，是单测里最隐蔽的「无效资产」。本仓的硬约束
             分三个角度覆盖最常见的虚假写法，存量为 0，规则上锁防止回潮：

             1) no_fake_assertion_constant：断言常量自身。
                ❌ assertTrue(true)            assertTrue(true, "msg")
                ❌ assertFalse(false)          assertFalse(false, () -> "msg")
                ❌ assertNull(null)
                这些断言不引用任何被测变量，唯一作用是让测试方法「看起来有断言」，
                典型出现在测试占位、TODO 删除残留、AI 生成代码兜底。
             2) no_fake_assertion_self_compare：左右两侧完全相同的等值/同一比较。
                ❌ assertEquals(x, x)          assertSame(Foo.BAR, Foo.BAR)
                ❌ assertEquals(1, 1)          assertNotEquals("a", "a")
                ❌ assertEquals("foo bar", "foo bar")
                同 token 自比恒真（或恒假），等同于没有断言。正则用反向引用 \1 严格匹配
                「首参与第二参字面相同」，复杂表达式（含括号/换行）不会误报。
             3) no_fake_assertion_assertj_literal：AssertJ 字面量自我断言。
                ❌ assertThat(true).isTrue()     assertThat(false).isFalse()
                ❌ assertThat(null).isNull()     assertThat(true).isNotNull()
                等同于角度 1，只是换了 fluent 写法。

             检测范围：覆盖 src/main 与 src/test/java——不仅限测试目录，是因为生产代码里
             写 `assertTrue(true)` 一样是 dead code（且通常是误删条件后的残余）。
             ignoreComments=true 让 javadoc 举反例不被误判。

             **不覆盖**的情况（靠 PR review 与单测覆盖率把关）：
             - `@Test` 方法整体没有任何断言（Checkstyle 的 regex 难以稳健地跨越方法体大括号）；
             - `assertNotNull(someJustConstructedObject)` 这类语义弱断言；
             - `verify(mock)` 完全没设期望的 mockito 调用——存在但无意义。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="no_fake_assertion_constant"/>
            <property name="format" value="\bassert(?:True\s*\(\s*true|False\s*\(\s*false|Null\s*\(\s*null)\s*[,)]"/>
            <property name="message" value="禁止虚假断言：assertTrue(true)/assertFalse(false)/assertNull(null) 不引用任何被测变量，等同于没有断言。请改为断言被测代码真实产生的值。"/>
            <property name="ignoreComments" value="true"/>
        </module>
        <module name="RegexpSinglelineJava">
            <property name="id" value="no_fake_assertion_self_compare"/>
            <property name="format" value="\bassert(?:Equals|Same|NotEquals|NotSame)\s*\(\s*([^,()\n]+?)\s*,\s*\1\s*[,)]"/>
            <property name="message" value="禁止虚假断言：assertEquals/assertSame 等左右两侧完全相同等同于没有断言。请让一侧是期望值字面量、另一侧是被测代码产生的实际值。"/>
            <property name="ignoreComments" value="true"/>
        </module>
        <module name="RegexpSinglelineJava">
            <property name="id" value="no_fake_assertion_assertj_literal"/>
            <property name="format" value="\bassertThat\s*\(\s*(?:true|false|null)\s*\)\s*\.\s*(?:isTrue|isFalse|isNull|isNotNull)"/>
            <property name="message" value="禁止虚假断言：assertThat(true).isTrue() 这类对字面量的 AssertJ 断言等同于没有断言。请把被测代码产生的实际值传入 assertThat(...)。"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- no_empty_catch_block: 禁止用「空 catch 块」静默吞掉异常。
             空 catch（含 `catch (Exception ignored) {}` 这种形式）让异常在生产环境彻底失声——
             日志拿不到、监控拿不到、复现也复现不出，故障溯源时只能靠猜。
             规则只放行一种合规写法：catch 体内有真实处理（log / throw / 中断恢复
             Thread.currentThread().interrupt() / 状态更新等）。仅留一行解释性注释**不**够——
             那只是"signed silence"，对运行时观测仍然是零。要让"无害 fall-through"可信，
             就显式 `log.debug("contextual message", e)` 走 SLF4J 链路。
             检测：Checkstyle 内置 EmptyCatchBlock 模块。exceptionVariableName 留默认 `^$`——
             即便变量起名 `ignored`/`expected` 也照样拒，强制让作者写日志。 -->
        <module name="EmptyCatchBlock">
            <property name="id" value="no_empty_catch_block"/>
            <property name="commentFormat" value=".*"/>
        </module>

        <!-- no_throw_runtime_exception: 禁止 `throw new RuntimeException(...)`。
             RuntimeException 是所有 unchecked 异常的根，过于笼统，永远无法表达任何方法
             自身的抽象层次——调用方拿到它既不能 catch 到特定语义，也无法区分这是
             I/O 失败、状态不变式被破坏，还是参数非法。
             推荐替换：
             - 包 IOException：`throw new UncheckedIOException(msg, e)`（本仓 5 处先例）
             - 状态不变式被破坏：`throw new IllegalStateException(msg)`（本仓 14 处先例）
             - 参数非法：`throw new IllegalArgumentException(msg)`
             - 领域错误：本仓已有 SkillLoadException / SessionPersistenceException 等
             正则锚定到 `throw new RuntimeException(`，子类名（`MyRuntimeException`）由
             `\b` 边界与严格匹配排除；catch 子句、`throws` 声明、`Class<? extends RuntimeException>`
             不会被误命中。其他三个根类（Exception / Throwable / Error）当前存量为 0，
             由 PR review 把关。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="no_throw_runtime_exception"/>
            <property name="format" value="\bthrow\s+new\s+RuntimeException\s*\("/>
            <property name="message" value="方法抛出的异常应与抽象层次匹配：禁止 throw new RuntimeException(...)。请改用 JDK 语义异常（UncheckedIOException、IllegalStateException、IllegalArgumentException、NoSuchElementException 等）或仓库领域异常（SkillLoadException、SessionPersistenceException 等）。"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- huge_cyclomatic_complexity: 圈复杂度超阈值即拒。
             switchBlockAsSingleDecisionPoint=true: 整段 switch 算 1 个决策点
             而非每 case 一个，避免合理的查表式 switch 被误判为「复杂」。
             阈值 20：让大多数协议解析 / mode dispatch 类「单一职责但分支多」的方法
             不必拆得过碎；真正超过 20 的方法基本都该拆。 -->
        <module name="CyclomaticComplexity">
            <property name="id" value="huge_cyclomatic_complexity"/>
            <property name="max" value="20"/>
            <property name="switchBlockAsSingleDecisionPoint" value="true"/>
        </module>

        <!-- huge_method: 方法的非空非（行）注释行数不得超过 50。
             countEmpty=false: 跳过空行；Checkstyle 同时跳过仅含 `//` 单行注释的行。
             仍计入：方法签名行、`}`、含代码的行尾注释、块注释 `/* ... */` 内部行。
             50 行是经验线：超过这条线的方法几乎都在做多件事，拆出子方法可读性
             明显更好；想保留就用 @SuppressWarnings("checkstyle:huge_method")
             并在 PR description 里写明保留理由。 -->
        <module name="MethodLength">
            <property name="id" value="huge_method"/>
            <property name="max" value="50"/>
            <property name="countEmpty" value="false"/>
            <property name="tokens" value="METHOD_DEF, CTOR_DEF, COMPACT_CTOR_DEF"/>
        </module>

        <!-- TopClassComment: public 类必须有 Javadoc 描述功能，且包含 @version 标签
             记录版本+创建日期。@since 由配套 Javadoc 自带，不强制校验（Checkstyle 没有
             标准 @since 校验项），但脚本会一同补齐。 -->
        <module name="MissingJavadocType">
            <property name="id" value="top_class_comment"/>
            <property name="scope" value="public"/>
        </module>
        <module name="JavadocType">
            <property name="id" value="top_class_comment_version"/>
            <property name="scope" value="public"/>
            <property name="versionFormat" value="\[br_eCampusCore [^,\]]+,\s*\d{4}/\d{2}/\d{2}\]"/>
            <!-- 不强制每个泛型/record 组件都要 @param —— 那超出 TopClassComment 范围。 -->
            <property name="allowMissingParamTags" value="true"/>
        </module>

        <!-- JavaComponentOrder: 文件按 copyright / package / import / 类 顺序排列且空行分隔。
             Java 语法已强制了相对顺序，这里主要约束「空行分隔」。 -->
        <module name="EmptyLineSeparator">
            <property name="id" value="java_component_order_blank_line"/>
            <property name="tokens" value="PACKAGE_DEF, IMPORT, STATIC_IMPORT"/>
        </module>
        <!-- 禁止 import xxx.* 与 import static xxx.*。 -->
        <module name="AvoidStarImport">
            <property name="id" value="java_component_order_no_star_import"/>
        </module>

        <!-- UpperEllRule (1/2): long 字面量必须用大写 L 后缀（避免小写 l 与数字 1 混淆）。 -->
        <module name="UpperEll">
            <property name="id" value="upper_ell_rule_l_suffix"/>
        </module>
        <!-- UpperEllRule (2/2): long 类型变量赋值整数字面量时，字面量必须以 L 结尾。
             正则匹配 `long <ident> = <digits>(_<digits>)*[ ;,)]`（无 L 后缀）。
             注意：Long 包装类型的拆箱赋值同样适用。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="upper_ell_rule_long_assignment"/>
            <property name="format" value="\b(?:long|Long)\s+\w+\s*=\s*-?\d+(?:_\d+)*\s*[;,)]"/>
            <property name="message" value="long 类型变量赋值的整数字面量必须以 L 结尾（如 60_000L），避免静默的 int→long 转换。"/>
            <property name="ignoreComments" value="true"/>
        </module>

        <!-- no_todo_fixme_in_delivery_code: 正式交付代码不允许 TODO/FIXME 注释。
             未完成工作改记录到仓内 docs/DEFERRED.md（或 issue tracker），
             代码里残留 TODO/FIXME 会让客户误解为已知缺陷。
             正则前置 // 或 /* 或 ` * `（Javadoc 续行），等价于「只检查注释」，
             避免字符串字面量 "TODO" 或标识符 todoList 误报。 -->
        <module name="RegexpSinglelineJava">
            <property name="id" value="no_todo_fixme_in_delivery_code"/>
            <property name="format" value="(?://|/\*+|\*\s)[^\n]*\b(?:TODO|FIXME)\b"/>
            <property name="message" value="正式交付代码不应包含 TODO/FIXME 注释。请把未完成工作改记录到 docs/DEFERRED.md。"/>
            <property name="ignoreComments" value="false"/>
        </module>

        <!-- one_variable_per_declaration: 每条 declaration 只能声明一个变量。
             禁止 `int a = 0, b = 0;` / `int m = a.size(), n = b.size();` 这类串联声明：
             一、diff 时新增/删除某个变量会污染其他变量行；
             二、IDE 提取/重命名时串联声明常被遗漏；
             三、类型与多份初始化逻辑挤在一行影响阅读。
             改成多行 `int a = 0;` `int b = 0;` 即可。
             同时覆盖类字段与方法局部变量；不限制 `for (int i = 0, j = 0; ...; ...)`
             这类 for 初始化器（Checkstyle 默认只检查 VARIABLE_DEF 不检查 FOR_INIT）。 -->
        <module name="MultipleVariableDeclarations">
            <property name="id" value="one_variable_per_declaration"/>
        </module>

        <!-- JavaDocFormat: 一旦方法写了 Javadoc，就必须严格合规——@param 覆盖所有参数、
             非 void 方法必须有 @return、@throws 必须覆盖签名声明的受检异常。
             仍然**不**强制每个方法都有 Javadoc（无 Javadoc 的方法不报错）。
             作用域覆盖所有访问级别：public / protected / package / private 都要遵守。 -->
        <module name="JavadocMethod">
            <property name="id" value="java_doc_format_consistency"/>
            <property name="accessModifiers" value="public,protected,package,private"/>
            <property name="allowMissingParamTags" value="false"/>
            <property name="allowMissingReturnTag" value="false"/>
            <property name="validateThrows" value="true"/>
            <property name="allowedAnnotations" value="Override,Generated,GeneratedBy,Bean"/>
        </module>

        <!-- no_complex_control_condition: 控制条件表达式（if/while/for/三元/return 的布尔
             条件）中的逻辑运算符总数不得超过 5 个——`&&`、`||`、`&`、`|`、`^` 合并统计。
             理由：单个布尔表达式里堆叠过多逻辑运算符（>5）说明判断逻辑已经被压成「一行
             代码 + 多重否定 + 优先级隐式约定」，读者必须心里画括号才能推理；拆成命名良好
             的局部 boolean 或私有 isXxx() 方法，每一步都能自解释、每一步都能独立单测、
             debugger 上单步看到中间值。5 个是经验线——少于 5 通常仍在可读区间。
             ❌ 反例（7 个运算符 → 拒）：
                 if (a && (b || c) && (d || e || f) && !g) { ... }
             ✅ 正例（拆 + 命名）：
                 boolean qualifies = a && (b || c);
                 boolean hasAccess = d || e || f;
                 if (qualifies && hasAccess && !g) { ... }
             由 Checkstyle 内置 BooleanExpressionComplexity 实现，max=5。 -->
        <module name="BooleanExpressionComplexity">
            <property name="id" value="no_complex_control_condition"/>
            <property name="max" value="5"/>
        </module>

        <!-- no_assignment_in_control_condition: 禁止把赋值表达式嵌进控制条件等更大的表达式
             里执行。最典型的反例是 `if ((x = next()) != null) { use(x); }`——一行里同时
             发生「赋值」和「条件判断」两件事，读者很容易把它误读成 `if (x == next())`（特别
             是 == vs = 的笔误），编译器无法分辨意图。
             ❌ 反例：
                 if ((line = reader.readLine()) != null) { ... }
                 while ((b = in.read()) != -1) { ... }
                 return x = compute();
             ✅ 正例（拆开赋值与判断）：
                 String line = reader.readLine();
                 while (line != null) {
                     ...
                     line = reader.readLine();
                 }
             由 Checkstyle 内置 InnerAssignment 实现。豁免：for 循环的 init/update 槽位、
             try-with-resources 资源声明——这两种语法上就要求在「条件位置」绑定变量。 -->
        <module name="InnerAssignment">
            <property name="id" value="no_assignment_in_control_condition"/>
        </module>

        <!-- AtclauseOrder: Javadoc 块标签必须按 @param → @return → @throws → @see → @since
             → @serial → @deprecated 顺序排列。理由：标准约定（JLS、Google Java Style、Oracle）
             一致要求这一顺序，IDE / Javadoc 工具按此渲染，与签名读取顺序一致（先读入参再读出参）。
             历史上出现过 `@return` 写在 `@param` 之前的批量生成代码，违反阅读直觉。 -->
        <module name="AtclauseOrder">
            <property name="id" value="javadoc_clause_order"/>
            <property name="tagOrder" value="@param, @return, @throws, @exception, @see, @since, @serial, @serialField, @serialData, @deprecated"/>
            <property name="target" value="CLASS_DEF, INTERFACE_DEF, ENUM_DEF, METHOD_DEF, CTOR_DEF, VARIABLE_DEF"/>
        </module>
    </module>
</module>
